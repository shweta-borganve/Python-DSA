name: Python CI

on:
  push:
    branches: ["main", "feature/*"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install isort mypy ruff bandit[toml] pip-audit coverage

      - name: Run code quality, type & security checks
        run: |
          echo "=== Running isort ==="
          isort --check .
          echo "=== Running black ==="
          black --check .
          echo "=== Running ruff ==="
          ruff check .
          echo "=== Running mypy ==="
          mypy src/
          echo "=== Running bandit ==="
          bandit -r src/
          echo "=== Running pip-audit ==="
          pip-audit

      - name: Run unit tests with coverage
        run: |
          echo "=== Running unit tests with coverage ==="
          mkdir -p coverage-output
          PYTHONPATH=. pytest --cov=src --cov-report=xml:coverage-output/coverage.xml

      - name: Run coverage gate
        uses: shweta-borganve/coverage-gate@v1
        with:
          coverage-file: coverage-output/coverage.xml
          threshold: "80"